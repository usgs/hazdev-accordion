'use strict';

var config = {
  build: '.build',
  devPort: 8040,
  dist: 'dist',
  distPort: 8042,
  example: 'example',
  liveReloadPort: 8049,
  src: 'src',
  test: 'test',
  testPort: 8041,
  templatePort: 8043
};

module.exports = config;
